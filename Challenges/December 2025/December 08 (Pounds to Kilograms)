""
Given a weight in pounds as a number, return the string "(lbs) pounds equals (kgs) kilograms.".

- Replace "(lbs)" with the input number.
- Replace "(kgs)" with the input converted to kilograms, rounded to two decimals and always include two decimal places in the value.
- 1 pound equals 0.453592 kilograms.
- If the input is 1, use "pound" instead of "pounds".
- If the converted value is 1, use "kilogram" instead of "kilograms".
""

# CODE #
def convert_to_kgs(lbs):
    kgs = round(lbs * 0.453592, 2) # Calculate the kilograms value
    if lbs == 1: pval = "pound" # If the pounds value is singular, set the plurality to singular
    else: pval = "pounds" # Otherwise, set the plurality to plural
    if kgs == 1: kval = "kilogram" # If the kilograms value is singular, set the plurality to singular
    else: kval = "kilograms" # Otherwise, set the plurality to plural
    while len(str(kgs).split(".")[1]) < 2: kgs = str(kgs)+"0" # If there are less than 2 values after the decimal, add 1 "0"
    return f"{lbs} {pval} equals {kgs} {kval}." # Return the information string
