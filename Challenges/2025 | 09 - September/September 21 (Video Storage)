"""
Given a video size, a unit for the video size, a hard drive capacity, and a unit for the hard drive, return the number of videos the hard drive can store using the following constraints:
- The unit for the video size can be bytes ("B"), kilobytes ("KB"), megabytes ("MB"), or gigabytes ("GB").
  - If not given one of the video units above, return "Invalid video unit".
- The unit of the hard drive capacity can be gigabytes ("GB") or terabytes ("TB").
  - If not given one of the hard drive units above, return "Invalid drive unit".
- Return the number of whole videos the drive can fit.
- Use the following conversions:
        1 B	  =  1 B
        1 KB	=  1000 B
        1 MB	=  1000 KB
        1 GB	=  1000 MB
        1 TB	=  1000 GB
"""

# CODE #
def number_of_videos(video_size, video_unit, drive_size, drive_unit):
    # Attempt to get the total number of bytes the video size is, and if fail, return "Invalid video unit"
    if video_unit == "B": pass
    elif video_unit == "KB": video_size*=1000
    elif video_unit == "MB": video_size*=1000*1000
    elif video_unit == "GB": video_size*=1000*1000*1000
    else: return "Invalid video unit"

    # Attempt to get the total number of bytes the drive size is, and if fail, return "Invalid drive unit"
    if drive_unit == "GB": drive_size*=1000*1000*1000
    elif drive_unit == "TB": drive_size*=1000*1000*1000*1000
    else: return "Invalid drive unit"

    return drive_size//video_size # Return the number of videos that can fit in the drive
