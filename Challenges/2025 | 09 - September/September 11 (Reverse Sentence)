"""
Given a string of words, return a new string with the words in reverse order.

- In the given string, words can be separated by one or more spaces.
- The returned string should only have one space between words.
"""

# CODE #
def reverse_sentence(sentence): return " ".join(filter(lambda x: x!="",sentence.split(" ")[::-1])) # Given a sentence, return the sentence after splitting it from the spaces, reversing it, removing the empty strings, and joining back to a string
