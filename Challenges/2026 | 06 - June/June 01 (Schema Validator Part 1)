"""
Given an object (JavaScript) or dictionary (Python), determine if it matches the following schema:

  { username: string }

Extra keys are allowed
"""

is_valid_schema = lambda obj: type(obj.get("username", 0)) == str
