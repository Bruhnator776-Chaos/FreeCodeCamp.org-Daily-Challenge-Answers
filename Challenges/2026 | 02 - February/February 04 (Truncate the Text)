"""
Given a string, return it as-is if it's 20 characters or shorter.
If it's longer than 20 characters, truncate it to the first 17 characters and append "..." to the end of it (so it's 20 characters total) and return the result.
"""

# CODE #

def truncate_text(text):
    if len(text) <= 20: return text # If the text is 20 characters or less, return the text unchanged
    else: return text[:17] + "..." # Otherwise, truncate the text to 17 characters and add "..." to the end, making it 20 characters
