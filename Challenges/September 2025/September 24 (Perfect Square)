"""
Given an integer, determine if it is a perfect square.

- A number is a perfect square if you can multiply an integer by itself to achieve the number.
"""

# CODE #
def is_perfect_square(n):
    if n < 0: return False # A negative number cannot be a perfect square

    for i in range(n+1): # For each number from 0-n...
        if i**2 == n: return True # If the number multiplied by itself is equal to n, then n is a perfect square
    return False # If n was never identified as a perfect square, n is not a perfect square
