"""
Given a sentence, return the longest word in the sentence.

- Ignore periods (.) when determining word length.
- If multiple words are ties for the longest, return the first one that occurs.
"""

# CODE #
def get_longest_word(sentence):
    sentence = sentence.split(" ") # Split the sentence into individual words.
    for word in sentence: # For each word in the sentence, format them then replace the originals in the sentence list with the formatted versions.
        if word.contains("."): sentence[sentence.index(word)] = word.strip()[:-1]
        else: sentence[sentence.index(word)] = word.strip()
    
    longest_word = "" # Create an empty string to hold the longest word in the sentence list.
    for word in sentence: # For each word in the sentence list
        if len(longest_word) < len(word): longest_word = word # If the current word is _longer_ than the longest recorded word, replace it. 

    return longest_word
