"""
Given a file size, a unit for the file size, and hard drive capacity in gigabytes (GB), return the number of files the hard drive can store using the following constraints:
- The unit for the file size can be bytes ("B"), kilobytes ("KB"), or megabytes ("MB").
- Return the number of whole files the drive can fit.
- Use the following conversions:
    1 B	  ===  1 B
    1 KB	===  1000 B
    1 MB	===  1000 KB
    1 GB	===  1000 MB
"""

# CODE #
def number_of_files(file_size, file_unit, drive_size):
    if file_unit == "B": drive_size*=1000*1000*1000 # If the file_unit is in Bytes, multiply the drive size to reflect that
    elif file_unit == "KB": drive_size*=1000*1000 # If the file_unit is in KiloBytes, multiply the drive size to reflect that
    elif file_unit == "MB": drive_size*=1000 # If the file_unit is in MegaBytes, multiply the drive size to reflect that
    return drive_size//file_size # Return the number of files that can fit in one drive
