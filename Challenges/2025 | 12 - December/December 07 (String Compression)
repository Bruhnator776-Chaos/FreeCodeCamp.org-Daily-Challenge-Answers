"""
Given a string sentence, return a compressed version of the sentence where consecutive duplicate words are replaced by the word followed with the number of times it repeats in parentheses.

- Only consecutive duplicates are compressed.
- Words are separated by single spaces.
"""

# CODE #
def compress_string(sentence):
    new_string, cword, count = [], None, 0 # Create an empty list to hold the values being returned, an empty variable to hold the current itterated word, and a count variable to hold the number of itterations the current itterated word has gone through
    for word in sentence.split(" "): # For each word in the sentence...
        if cword != word: # If the current word is not equal to the word,
            if cword == None: pass # If the current word is None, ignore the current if statement
            elif count == 1: new_string.append(cword) # If the count is 1, add the current word to the value list WITHOUT a count attached
            else: new_string.append(f"{cword}({count})") # Otherwise, add the current word to the value list WITH a count attached
            cword, count = word, 1 # Set the current word to the word, and the count variable reset to 1
        else: count += 1 # Otherwise, add one to the count
    if count == 1: return " ".join(new_string) + f" {cword}" # If the final word has a count of 1, return the new_string with the current word appended to the end WITHOUT a count
    else: return " ".join(new_string) + f" {cword}({count})" # Otherwise, return the new_string with the current word appended to the end WITH a count
