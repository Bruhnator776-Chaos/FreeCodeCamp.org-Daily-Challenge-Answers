"""
Given a positive integer, return the sum of all its divisors.

- A divisor is any integer that divides the number evenly (the remainder is 0).
- Only count each divisor once.
"""

# CODE #

def sum_divisors(n):
    divisors = [] # Create an empty list to hold each divisor
    for i in range(n): # For each number from 1-n inclusive...
        if n % (i+1) == 0: divisors.append(i+1) # If the number is a divisor to n, add it to the divisors list
    return sum(divisors) # Add up all of the numbers in the divisors list and return the sum.
