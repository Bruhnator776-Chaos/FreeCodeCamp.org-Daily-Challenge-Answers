"""
This is the 100th coding challenge.

Given a string, repeat its characters until the result is exactly 100 characters long.
If your repetitions go over 100 characters, trim the extra so it's exactly 100.
"""

# CODE #
def one_hundred(chars):
    while len(chars)<100: chars+=chars # While there are less than 100 characters in the string, add the string to itself
    return chars[:100] # Return the first 100 characters of the string
