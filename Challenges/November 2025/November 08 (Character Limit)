"""
In this challenge, you are given a string and need to determine if it fits in a social media post.
Return the following strings based on the rules given:
- "short post" if it fits within a 40-character limit.
- "long post" if it's greater than 40 characters and fits within an 80-character limit.
- "invalid post" if it's too long to fit within either limit.
"""

# CODE #
def can_post(message):
    if len(message) <= 40: return "short post" # If there are less than or equal to 40 characters in the post, return "short post"
    elif len(message) <= 80: return "long post" # If there are less than or equal to 80 characters in the post, and the above line did not pass, return "long post"
    else: return "invalid post" # Otherwise, return "invalid post"
