"""
Given two integers, the par for a golf hole and the number of strokes a golfer took on that hole, return the golfer's score using golf terms.

Return:
- "Hole in one!" if it took one stroke.
- "Eagle" if it took two strokes less than par.
- "Birdie" if it took one stroke less than par.
- "Par" if it took the same number of strokes as par.
- "Bogey" if it took one stroke more than par.
- "Double bogey" if took two strokes more than par.
"""

# CODE #

def golf_score(par, strokes):
    if strokes == 1: return "Hole in one!" # If only one stroke was taken, return "Hole in one!"
    elif strokes == par-2: return "Eagle" # If the stroke count is two less than par, return "Eagle"
    elif strokes == par-1: return "Birdie" # If the stroke count is one less than par, return "Birdie"
    elif strokes == par: return "Par" # If the stroke count is the same as par, return "Par"
    elif strokes == par+1: return "Bogey" # If the stroke count is one more than par, return "Bogey"
    else: return "Double bogey" # Otherwise, return "Double bogey"
