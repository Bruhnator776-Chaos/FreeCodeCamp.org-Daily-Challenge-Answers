"""
Given an array of integers, return an array of integers that appear more than once in the initial array, sorted in ascending order.
If no values appear more than once, return an empty array.

- Only include one instance of each value in the returned array.
"""

# CODE #
def find_duplicates(arr):
    duplicates = [] # Create an empty list to hold the duplicate entries
    for item in set(arr): # For each item in a set form of the array
        if arr.count(item) > 1: duplicates.append(item) # If the item is found multiple times within the array, add it to the list of duplicates
    return sorted(duplicates) # Return a sorted list of duplicates
